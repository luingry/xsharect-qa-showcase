# Contributing

Keep changes synthetic, deterministic and reviewable. Do not import assets, strings, logs, identifiers or code from private products. Run `npm ci`, `npm test`, and `npm run test:critical` before proposing a change.
